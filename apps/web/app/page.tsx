import { RiskReview } from "../components/RiskReview";
const goals = [{ name: "Medicine Fund", saved: 3200, target: 10000 }, { name: "Emergency Fund", saved: 5400, target: 15000 }];
export default function Home() { return <main className="mx-auto max-w-4xl p-5 sm:p-10"><header><p className="text-lg font-semibold text-safety">Tolveris Launchpad</p><h1 className="mt-2 text-4xl font-bold tracking-tight">Bank with confidence.</h1><p className="mt-3 max-w-2xl text-xl text-slate-700">Simple savings, short safety lessons, and a helpful check before sending money.</p></header><section className="mt-8 rounded-2xl bg-safety p-7 text-white"><p className="text-lg">Available balance</p><p className="mt-1 text-4xl font-bold">PHP 12,450.00</p><div className="mt-6 flex flex-wrap gap-3"><button className="rounded-xl bg-white px-5 py-3 font-bold text-safety">Add money</button><button className="rounded-xl border border-white px-5 py-3 font-bold">Send money</button></div></section><section className="mt-8"><h2 className="text-2xl font-bold">My Ipon goals</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{goals.map(goal => <article className="rounded-2xl bg-white p-5 shadow-sm" key={goal.name}><h3 className="text-xl font-bold">{goal.name}</h3><p className="mt-3 text-lg">PHP {goal.saved.toLocaleString()} of PHP {goal.target.toLocaleString()}</p><div className="mt-3 h-4 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-emerald-500" style={{ width: `${goal.saved / goal.target * 100}%` }} /></div><button className="mt-4 rounded-lg border-2 border-safety px-4 py-2 font-semibold text-safety">Add to goal</button></article>)}</div></section><div className="mt-8"><RiskReview /></div></main>; }
